<div class="mt-4">
  <div class="row gy-3">
    <% @games.each do |game|%>
      <div class="col-sm-12 col-md-6 col-lg-4">
        <%= link_to game_path(game), class: "car-link" do%>
          <%= render "shared/card_games", game: game%>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
